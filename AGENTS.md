# AGENTS.md

本文件为工作区操作规则：agent 在本工作区如何工作、优先级与必须避免的事项。

## 启动上下文

- 阅读 `SOUL.md` 了解稳定的人设与协作风格。
- 阅读 `USER.md` 了解用户稳定画像（私有会话时）。
- 阅读 `TOOLS.md` 了解工作区工具约定。
- 遵循 `BOOTSTRAP.md`（如存在），设置完成后删除。

## 工作规则

- 更高优先级的系统、开发者、运行时、安全与用户直接指令覆盖本文件。
- 改动保持小、可回退，且与用户请求直接相关。
- 工作流规则、常驻指令与操作边界写在这里。
- 不在此存放用户画像（用 `USER.md`）、助手人设（用 `SOUL.md` / `IDENTITY.md`）、长期记忆（用 `MEMORY.md` / `memory/**/*.md`）、密钥与一次性任务笔记。
- 在共享、频道、定时或子代理上下文中，除非运行时策略明确放开，否则避免暴露私有用户或长期记忆内容。
- 编写提交信息、PR 标题/正文、发布说明等项目对外变更摘要时，只描述产品变更、理由与验证；不提及 agent 内部工作流与工具链名称（如 Superpowers、OMX、Codex、Claude 等），除非用户明确要求。

## AI 生成代码必须遵守的规则（用户指定，除非用户明确指出，否则一律遵守）

### 通用

- 代码编辑/修改不要采用兜底机制（不要靠降级、静默吞错、猜默认值等方式兜底）。
- 所有代码添加易于新手理解的中文注释。

### 数据库

- 禁止 N+1 查询（循环内逐条查库），必须用批量查询、预加载或 JOIN 等方式一次取齐。

### HTML

- 禁止使用 `aria-label`、`stroke-width`、`aria-hidden` 属性。

### CSS

- 禁止使用渐变色、阴影、伪元素、动画、`duration`。
- 命名禁止使用包含 `__` 双下划线的写法（如 BEM 的 `block__element`）。
- 使用 Tailwind CSS 编写样式，统一使用 flex 布局；能简单实现的效果就用简单方式实现。

## 行为指令

### 写代码时

- 每个功能用最简单直接的方式实现。
- 不引入不必要的设计模式，除非用户明确要求。
- 不做过度抽象。
- 不引入技术栈以外的依赖，需要时先询问用户。
- 所有外部调用必须有超时设置。

### 改代码时

- 先理解相关模块的设计意图。
- 不要为了新功能破坏已有接口契约。
- 改完确保已有测试通过。

### 不确定时

- 架构选择给出 2-3 个方案对比，由用户拍板。
- 规范未覆盖的情况，先询问用户，不要自行编造规矩。
